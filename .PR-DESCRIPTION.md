# PR — feat(m7): depend on resilience-kit, delete embedded kit code (phase 3)

> Branch `feat/depend-on-resilience-kit` is pushed to origin. Open the
> PR at https://github.com/prajwalmahajan101/django_boilerplate/pull/new/feat/depend-on-resilience-kit
> and paste the body below. `gh pr create` failed locally with `HTTP 401`
> — the cached token needed a refresh I wasn't allowed to perform.

---

## Summary

Migrates `django_boilerplate` to depend on the published
[`resilience-kit==0.1.0rc1`](https://pypi.org/project/resilience-kit/0.1.0rc1/)
and deletes the embedded copy of the kit's surface. M7 Phase 3 of the
shared kit's roadmap — the FastAPI sibling landed first as
[`prajwalmahajan101/fastapi_boilerplate#6`](https://github.com/prajwalmahajan101/fastapi_boilerplate/pull/6).

10 atomic commits, conventional commit prefixes, one logical change each:

| # | Commit | What |
|---|---|---|
| 1 | `chore(deps)` | Pin kit + regen all four `pip-tools` locks with hashes. |
| 2 | `feat(settings)` | `INSTALLED_APPS += resilience_kit.adapters.django`; collapse `RATE_LIMIT_CONFIG` / `CIRCUIT_BREAKER_CONFIG` / `RESILIENCE_DEFAULTS` / `FIELD_ENCRYPTION_KEY` into a single `RESILIENCE` dict. |
| 3 | `refactor(middleware)` | Swap six kit-owned middleware classes (`SelectiveCors`, `SecurityHeaders`, `BodyLimit`, `RequestId`, `ExceptionLogging`, `RateLimitHeaders`) to `resilience_kit.adapters.django.middleware.*`. Keep `RequestLoggingMiddleware` (boilerplate). |
| 4 | `refactor(core)` | Drop `_start_recovery_monitor` from `CoreConfig.ready` — kit's `ResilienceConfig.ready()` owns the thread. |
| 5 | `refactor(throttling)` | `DEFAULT_THROTTLE_CLASSES` → `resilience_kit.adapters.django.drf_throttles.{UserTier,Burst}Throttle`. `GlobalThrottle` retired (kit has no equivalent; nginx covers system-wide cap). |
| 6 | `refactor(core/base)` | `EncryptedCharField` becomes a re-export from `resilience_kit.adapters.django.fields`. Django autodetector confirms no migration churn. |
| 7 | `feat(exceptions)` | **Bridge**: `BaseCustomError → ResilienceKitError`; **compose**: boilerplate handler still wraps the envelope, delegates to kit `handle()` for raw kit exceptions. |
| 8 | `refactor` | Codemod survivors (`accounts/views`, `core/views`, `utils/{ses,s3}`, `registries/__init__`, `tests/conftest`) to point at kit primitives. |
| 9 | `chore` | Delete embedded `core/resilience/`, kit-owned middleware files, `utils/{http_client,crypto}`, `lifecycle/healthcheck` (recovery dispatch retargeted), `testing/`, `runtime.py`. |
| 10 | `test` | Drop kit-owned suites (~12 files); rewire `test_csp_report` + `test_exception_handler`; `RESILIENCE_CRYPTO__ENVIRONMENT=dev` in test settings; fix `details` property collision. |

## ROADMAP M7 exit gate

> **Exit when.** Both PRs open with green local test runs; embedded
> `core/resilience/` and friends are gone from both repos. Tag
> `milestone/m7` on this repo's `main` after both boilerplate PRs merge.

— `resilience-kit/docs/ROADMAP.md` §M7.

This PR satisfies:
- ✅ Embedded `apps/core/resilience/` deleted (entire tree + kit-owned middleware + `utils/http_client` + `utils/crypto` + `lifecycle/healthcheck` + `testing/` + `runtime.py`).
- ✅ Local `pytest` green — **171 passed** (3 pre-existing `TestCompositeAuthentication` failures unrelated; see *Known issues*).
- ✅ `./manage.py check` clean.
- ✅ `./manage.py migrate --check` clean against a real Postgres.

## Deviations from `docs/MIGRATION-from-boilerplate-embedded.md`

Five kit-side gaps surfaced during the migration; all logged in
[`docs/m7-kit-integration-notes.md`](./docs/m7-kit-integration-notes.md)
and feed the kit integration report (see follow-ups):

1. **No `DjangoSettingsSource`** — kit's `ResilienceConfig.ready()` only
   reads `RESILIENCE["services"]`. The `RESILIENCE` dict's other keys
   are documentation, not config. Env vars (`RESILIENCE_*`) remain the
   only override surface.
2. **No `resilience_kit.utils` sub-package** — MIGRATION promised
   `utils.{log_sanitization,network,timing,function_logger,data}`
   re-exports. None exist. Kept locally; not deleted.
3. **No `GlobalThrottle`** — kit ships only `IP/UserTier/Burst/Endpoint/Auth`.
   Dropped `GlobalThrottle` from `DEFAULT_THROTTLE_CLASSES`.
4. **`core.metrics` surface incompatible** — kit's Protocol-based
   `MetricsSink` is not a drop-in for the boilerplate's free-function
   shim + bounded-label contract. Kept `core.metrics`.
5. **Exception bridge required** — kit's `ExternalServiceError` etc.
   inherit `ResilienceKitError`, not `BaseCustomError`. Bridge
   (`class BaseCustomError(ResilienceKitError)`) keeps every domain
   subclass envelope-aware. Composition wrapper in `handler.py` makes
   `EXCEPTION_HANDLER` work without breaking the envelope contract.

## Known issues (not migration-caused)

- `apps/core/tests/test_auth_registry.py::TestCompositeAuthentication`
  has three pre-existing failures (introduced in `main` commit
  `6be5dcf perf(auth)`, MagicMock auto-attribute collision with the
  per-request snapshot). Reproduces against `main` HEAD; out of scope
  for this PR.

## Follow-ups

- Integration audit + improvement report fed back to the kit
  (`docs/m7-kit-integration-report.md`).
- Tag `milestone/m7` on the kit repo after merge.
- Re-pin `0.1.0rc1 → 0.1.0` once the kit's M8 release lands.

## Test plan

- [ ] CI / local pytest green (171/174 passing; 3 pre-existing failures documented).
- [ ] `./manage.py check` clean.
- [ ] `./manage.py migrate --check` clean against project Postgres.
- [ ] Manual smoke: `runserver` boots, `/api/auth/google/`, `/api/health/`, `/api/readiness/`, `/api/csp-report/` reachable.
- [ ] `EncryptedCharField` round-trip via shell against an existing `APIKey` row (cipher path unchanged).
